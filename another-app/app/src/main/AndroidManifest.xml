<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seoft.co.kr.another_app">

    <permission android:name="seoft.co.kr.another_app.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="seoft.co.kr.another_app.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider android:name=".StudentsProvider"
            android:authorities="seoft.co.kr.another_app.StudentsProvider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="seoft.co.kr.another_app.READ_DATABASE"
            android:writePermission="seoft.co.kr.another_app.WRITE_DATABASE" />

    </application>

</manifest>