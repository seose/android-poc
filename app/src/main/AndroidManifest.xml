<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seoft.co.kr.android_poc">

    <uses-permission android:name="seoft.co.kr.another_app.READ_DATABASE" />
    <uses-permission android:name="seoft.co.kr.another_app.WRITE_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<provider android:name=".StudentsProvider"-->
            <!--android:authorities="seoft.co.kr.android_poc.StudentsProvider"-->
            <!--android:enabled="true"-->
            <!--android:exported="true"-->
            <!--android:permission="seoft.co.kr.android_poc.READ_WRITE" />-->

    </application>

</manifest>